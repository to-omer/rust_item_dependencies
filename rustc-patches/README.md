# rustcパッチ

このディレクトリには、`rust-item-dependencies`がrustcから依存関係を取得するためのパッチを置いています。通常の利用では、`cargo rid`がパッチの適用とコンパイラの準備を自動で行います。

`series`にはパッチを適用する順序を記録しています。ほかの管理ファイルは、対応するrustcとパッチの組み合わせをツールが判別するために使います。
