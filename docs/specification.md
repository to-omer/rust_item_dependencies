# rust-item-dependenciesの仕様

## 対象

安定版Rustで書かれた、`main`関数を持つ1ファイルの実行プログラムを対象とします。Rustのエディションは2015、2018、2021、2024に対応します。

## 入力

入力はUTF-8のRustソースコード、Rustのエディション、コンパイル対象、最適化レベル、名前形式の`cfg`の集合です。コンパイル対象はrustcに組み込まれており、使用するコンパイラで標準ライブラリを利用できる必要があります。

最適化レベルは`0`、`1`、`2`、`3`、`s`、`z`に対応し、省略時は`0`です。`debug_assertions`は、最適化レベルに基づいてRustコンパイラが決めます。名前形式の`cfg`は複数指定できます。

値付き`cfg`、`debug_assertions`の独立した指定、Rustコンパイラが外部指定を禁止する組み込み`cfg`には対応していません。コンパイル対象や最適化レベルからRustコンパイラが決める組み込み`cfg`は、そのまま利用できます。

外部ファイル、外部クレート、手続きマクロ、コンパイル時の外部リソース、ネイティブライブラリの明示的なリンクを必要とするコードは対象外です。`no_std`、`no_main`、unstable機能、インラインアセンブリ、独自の実行基盤を使うコードも扱いません。

## 依存関係の判定

ソースコード上の名前だけではなく、コンパイラが確定した名前解決、型と`impl`の選択、マクロ展開、生成されるコードの依存関係を使います。

`main`とコンパイラが必要とする要素を起点に依存関係をたどり、到達したソースコードの構成要素を残します。判断に必要な情報が不足している場合は、推測して削除せずエラーを返します。

## 削減する単位

関数、型、`impl`、`trait`のメソッドや関連項目、`use`の枝、マクロ呼び出し、`macro_rules!`の未使用ルールなど、ソースコード上で安全に区切れる範囲を削減単位として扱います。フィールド、列挙型のバリアント、関数本体内の一般の文や式は、単独では削除しません。

マクロが生成したコードは、元のマクロ呼び出しとの関係を使って扱います。展開後の断片だけを元ソースから無理に削除することはしません。

## 出力

出力は削減後のRustソースコードです。元の入力ファイルは変更しません。削除した範囲の前後にある空白や改行は、そのまま残る場合があります。

CLIは`-o`で指定した新しいファイルへ書きます。入力ファイルと同じ場所や、すでに存在するファイルは指定できません。

## エラー

入力が対象外である場合、元のコードがコンパイルできない場合、または安全に削除範囲を確定できない場合は、削減結果を返しません。

## 保証の範囲

このツールが保証するのは、対応する入力について、コンパイラが依存すると判断したコードを残すことです。

あらゆる入力に対する実行時の動作が完全に同じであることは証明しません。コードを物理的に削除するため、行番号、列番号、診断位置、`line!`や`column!`など位置に依存する値が変わることがあります。

## 対応環境

公開CLIはホスト環境を自動判定し、同じコマンドで専用コンパイラの準備と削減を行います。生成したコンパイラ、ビルドキャッシュ、一時的な成果物はすべてリポジトリの`target/`配下に置きます。
